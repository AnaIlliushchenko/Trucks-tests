# Trucks-tests based on Cypress
## Setup

1. Install packages
```
npm i
```
2. Install Cypress
```
npm install cypress --save-dev
```
## Running the Example Test in UI Mode

Run your tests with UI Mode.
```
npx cypress open
```
